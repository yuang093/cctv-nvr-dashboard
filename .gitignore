# === 機密設定（絕對不要 commit） ===
.env
.env.*
!.env.example

# === 真正的 nvr_config（帳密明碼，建議改用 .env 覆蓋） ===
nvr_config.json

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# === 掃描輸出 ===
*.db
*.db-journal
*.log

# === 歸檔的 PDF 報告（每次掃描自動產生，不入版控） ===
reports/

# === 一次性維運工具（任務完成後不需版控） ===
dedup_nvr_db.py
verify_dedup_after_cleanup.py
cleanup_orphan_nvr1.py
# 2026-08-03 整理：補一批一次性工具（重構 / dump / 探勘）
rebuild_app.py
reconstruct_app.py
reconstruct_v2.py
seed_resolved_test.py
discover_clip_origin.py
discover_media_api_8443.py
# 一次性 fix 紀錄（8 個 txt）
fix01.txt
fix02.txt
fix03.txt
fix04.txt
fix05.txt
fix06.txt
fix07.txt
fix08.txt
# 一次性 dump（重構時整個 app.py 內容）
full_app_read.txt
reconstructed_app_lines.txt
reconstructed_app_section.txt
# 一次性探勘輸出
6.0
API.txt
web_endpoint_api_2026-05-15-16-34-39.pdf

# === 執行階段 artifacts（2026-08-03 補）===
# Flask / Playwright / PyInstaller 輸出
*.out
*.err
*.exe
*.rar
.playwright-mcp/
.playwright-report/
build/
dist/
deploy/

# === 規格設計原始 mockup（保留 021.PNG 在 docs）===
# 其它 .png 截圖選擇性 add（驗證測試結果）；不強制 ignore

# === 歷史開發截圖（001-025）：開發過程探索產生，無對應文件 ===
# 保留磁碟檔案但不入版控；未來需要時可手動 `git add -f` 恢復
0[0-9][0-9].PNG
0[0-9][0-9].png

# === 廠商使用手冊 / 中文參考文件（不入版控） ===
Arisan-Gateway-*.pdf
文件資訊.txt


