---
tags:
  - devices
summary: 異常事件篩選清單
parameters:
  - name: hours
    in: query
    type: integer
    default: 24
  - name: nvr_id
    in: query
    type: integer
  - name: topic
    in: query
    type: string
  - name: status
    in: query
    type: string
    enum: [all, open, resolved]
    default: all
responses:
  200:
    description: events.html 渲染
